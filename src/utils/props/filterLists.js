const textInputOptions = [
  "autocomplete",
  "autofocus",
  "disabled",
  "name",
  "placeholder",
  "readOnly",
  "type",
  "value",
];

const filterOptions = {
  textInputOptions,
};

export default filterOptions;
